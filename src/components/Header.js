import React from 'react';
import Nav from './Navbar'

const Header = () => {
    return (
        <header>
            <h1>Pokemon App</h1>
            <Nav />
        </header>
    );
};

export default Header;